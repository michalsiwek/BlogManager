@model BlogManager.Models.ForgotPasswordViewModel
@{
    ViewBag.Title = "Forgot your password?";
}

<br />
<h2>@ViewBag.Title</h2>
<div class="row">
    <div class="col-md-8 col-md-offset-2">
        <section>
            @using (Html.BeginForm("ProceedPasswordReset", "Account", FormMethod.Post, new { @class = "form-horizontal" }))
            {
                @Html.AntiForgeryToken()
                <h4 class="text-center">Type the registration email</h4>
                <hr />
                @Html.ValidationSummary(true, "", new { @class = "text-center" })
                <div class="form-group row">
                    @Html.LabelFor(m => m.Email, new { @class = "col-md-4 col-sm-4 col-xs-4 control-label text-right" })
                    <div class="col-md-8">
                        @Html.TextBoxFor(m => m.Email, new { @class = "col-md-6 col-sm-6 col-xs-6 form-control" })
                        @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger" })
                    </div>
                </div>
                <hr />
                <div class="form-group">
                    <div class="text-center">
                        <input type="submit" class="btn btn-primary" value="Proceed" />
                    </div>
                </div>
            }
            <form class="text-center form-horizontal"></form>
        </section>
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}